alias \
  sudo='sudo ' \
  c='calcurse' \
  lw='librewolf' \
  l='clear' \
	scsc='sudo cryptsetup close' \
	scso='sudo cryptsetup open' \
	p='pacman' \
	lf='nnn -deR' \
	e='$EDITOR' \
	x='startx' \
	ls='ls --color=always' \
	grep='grep --color=always' \
  es='ecasound -c' \
  er='ecasound -c -s *.ecs' \
	m='mutt' \
	lg='lazygit' \
	sdn='shutdown -h now' \
	smci='sudo make clean install' \
	lb='lsblk' \
	otp='pass otp code -c' \
	nb='newsboat' \
	z='zathura' \
  cwd='pwd | xclip && echo "file path copied"' \
	om='wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle' \
	ov='wpctl set-volume @DEFAULT_AUDIO_SINK@' \
	im='wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle' \
	iv='wpctl set-volume @DEFAULT_AUDIO_SOURCE@' \
  vol='wpctl status | grep Stereo' \
	wfl='nmcli dev wifi list' \
	wfc='nmcli dev wifi connect -a' \
	edu='pass gu -c && nmcli dev wifi connect eduroam -a' \
	vpn='systemctl status wg-quick@wg0.service' \
	vpn0='systemctl stop wg-quick@wg0.service' \
	vpn1='systemctl start wg-quick@wg0.service' \
  ea='$EDITOR ~/.dotfiles/.config/shell/aliases' \
  lgr='zathura ~/doc/uni/lit/Lgr22.pdf' \
  apa='zathura ~/doc/uni/lit/APA7-lathund.pdf' \
  kanon='zathura ~/doc/uni/lit/sou-202592.pdf' \
  sa='source ~/.dotfiles/.config/shell/aliases' \
  add='git add' \
  amend='git commit --amend -m' \
  commit='git commit -m' \
  gdiff='git diff' \
  gst='git status | less' \
